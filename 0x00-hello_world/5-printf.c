 #include <stdio.h>
/**
 * main - The entry point for all C programs
 *
 * @void: does not take parameters
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
			return (0);
}
